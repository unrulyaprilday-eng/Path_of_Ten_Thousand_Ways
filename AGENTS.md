# AGENTS.md

## 项目身份

你是本项目的独立游戏开发协作者。项目目标是一款「修仙 RPG + Roguelike 节点式肉鸽 + 卡牌构筑 + 半即时斗法 + 局外成长」独立游戏。

项目核心一句话：

> 修仙不是打怪升级，而是构筑一条属于自己的道途。

## 目录约定

- `Doc/`：设计文档目录，是所有实现判断的第一依据。
- `UNITY/`：Unity 工程目录。准备或实现 DEMO 时，可以在项目根目录创建并使用该目录。
- `AGENTS.md`：开发协作规则。后续自动化代理、AI 协作者和人工开发者都应优先阅读本文件。

## 协作边界说明

本项目不是纯代码项目，而是「代码实现 + 玩法验证 + 美术资源生产」并行推进的独立游戏项目。

- 当任务涉及 UI 背板、卡框、图标、战斗背景、Boss 立绘、场景图、宣传图等视觉资产时，应视为正常开发流程的一部分。
- 这类任务默认允许并鼓励使用 AI 绘图 / 图像生成能力推进，只要产物符合 `Doc/` 中的美术方向、命名规范和资源优先级。
- 在 Codex 会话内，凡是需要新建或编辑位图类视觉资产，默认优先使用内部挂载的 `image_gen` / `imagegen` 能力；如果该能力不可用、调用失败或不适合当前需求，再使用项目可用的 `imagegen2` skill 作为备用生图通道，而不是把需求误判为只能手写占位、只能延后、或只能依赖外部模型网站。
- 不要因为项目仍在 DEMO 阶段，就把一切视觉资源都推迟到“最后再做”；只要某张图能显著提升当前可玩验证、观感判断或演出反馈，就应及时生成并纳入版本。
- 只有在现有代码原型已经足够验证目标、且该视觉资源不会影响本轮判断时，才可以延后正式出图。

## 文档读取规则

不要在开发流程里写死设计文档路径。`Doc/` 内文档可能会重命名、重新编号或拆分。

每次开始任务时：

1. 先阅读 `Doc/README.md`。
2. 根据 README 的目录索引，选择本次任务相关文档。
3. 做 DEMO 第一阶段相关工作时，优先阅读 README 中的「DEMO 第一阶段设计」。
4. 涉及完整系统判断时，再阅读 README 中对应的主设计文档、数值圣经或系统设计文档。
5. 如果 README 与具体文档冲突，以更具体、更新、更贴近当前任务目标的文档为准，并在开发备注中记录取舍。

建议阅读顺序：

- 项目定位类任务：先读 README，再读项目总设计、核心框架、项目级 GDD。
- 战斗原型类任务：先读 README，再读 `03_战斗系统与表现`、`05_状态与多目标规则`、`04_修行构筑与境界`。
- 数值类任务：先读 README，再读 `06_数值奖励与局外解锁`、`04_修行构筑与境界`、`05_状态与多目标规则`。
- 肉鸽流程类任务：先读 README，再读 `02_DEMO旅程地图与叙事`、`06_数值奖励与局外解锁`、`07_配置数据与存档契约`。
- 资源生产类任务：先读 README，再读 `08_UI美术与资源生产规范`、`11_资源制作单与开发记录`、`09_开发验证规范`。

## DEMO 第一阶段目标

第一阶段优先实现可玩的核心验证 DEMO，而不是完整商业版本。

DEMO 的范围以 `Doc/README.md` 指向的「DEMO 第一阶段设计」为准。没有更具体说明时，默认先验证：

- 半即时卡牌战斗：规划阶段 + 执行阶段。
- 玩家通过手牌进行低频决策，系统自动执行飞剑、法术、Buff 和法宝联动。
- 至少 1 条明确流派，优先做「剑修 / 飞剑流」。
- 基础卡牌、飞剑自动攻击、敌人受击反馈、回合循环、胜负结算。
- 一条可完整走通的旧矿三幕旅程：每幕实际经过 6-8 个以上节点，包含战斗、事件、修炼、秘藏、小 Boss、筑基与区域 Boss。
- 玩家能感受到「构筑成型 -> 联动触发 -> 爆发清屏」的核心体验。

DEMO 暂不优先追求：

- 完整局外宗门成长。
- 大量卡牌、遗物、装备和境界。
- 完整剧情、全量正式美术资产和商业化系统。
- 多流派平衡。

说明：

- 这里的「暂不优先追求」指的是不追求全量、终版、商业级资源覆盖。
- 不代表禁止或延后当前 DEMO 所需的关键美术资产生产。
- 不代表 DEMO 可以长期停留在“程序占位 UI”或“只有功能没有门面”的状态。
- 对外展示、首屏判断、核心玩法观感验证所需的 UI，应默认按正式产品卖相推进。
- 对验证战斗观感、UI 层次、节点地图识别、Boss 压迫感有直接帮助的资源，应按 P0 / P1 需求及时生成。

## 开发优先级

P0：

- 战斗系统
- 卡牌系统
- 飞剑自动攻击
- 基础敌人和 Boss
- 节点式肉鸽流程
- 场景化能力获得与修行选择

P1：

- 法宝规则改变器
- 遗物系统
- 状态系统
- 流派权重掉落
- 简化局外成长

P2：

- 装备系统
- 完整境界体系
- 图鉴、经济、商店扩展
- 社交、公会、排行榜等长期内容

## Unity 实现约定

如果创建 Unity 工程：

- 工程目录使用 `UNITY/`。
- 推荐 Unity 当前 LTS 稳定版，除非项目已有版本锁定。
- 优先实现 2D / 2.5D 横版空中斗法，不要一开始转向高成本 3D ARPG。
- 战斗表现使用低成本高反馈方案：分层背景、角色浮空、飞剑轨迹、卡牌驱动特效、屏幕震动和受击反馈。
- 核心数据尽量数据驱动，例如 ScriptableObject、JSON 或表格导入，避免把卡牌和数值硬编码在战斗逻辑里。

建议 Unity 目录结构：

```text
UNITY/
  Assets/
    Art/
    Audio/
    Data/
    Materials/
    Prefabs/
    Scenes/
    Scripts/
      Battle/
      Cards/
      Common/
      Combatants/
      Map/
      Rewards/
      Systems/
      UI/
    VFX/
  ProjectSettings/
```

## 代码风格与架构原则

- 先做可玩的闭环，再扩展内容量。
- 保持系统边界清晰：卡牌、战斗结算、单位、飞剑、状态、奖励、地图节点不要混成一个大脚本。
- 当某段程序、脚本、配置读取、工具函数或生成流程已经在两个以上模块重复出现，或明显会被继续复用时，应优先沉淀为公共库 / 公共模块，而不是继续复制粘贴造轮子。
- 通用能力优先放在清晰的公共位置，例如 `UNITY/Assets/Scripts/Common/`、`Systems/` 或专门的工具脚本目录，并保持接口简洁、命名稳定、易于被后续功能直接复用。
- UI 可以先控制范围，但不能长期停留在纯调试排版；凡是承担首屏门面、战斗信息层级、奖励识别和道途选择的界面，都应尽快向正式商业化展示标准收束，同时完整表达当前战斗状态：生命、灵气、回合阶段、手牌、敌人状态、奖励结果。
- UI 开发默认朝商业化产品标准靠拢：简洁、明了、可读性强，优先保证信息层级、对比、留白和交互辨识，再追求装饰细节。
- 当正式 UI 资源暂时缺失时，允许并鼓励先用色块、纯色面板、简化描边和占位块完成布局验证；在布局、层级和可读性确认通过后，再生成或接入正式图片资源。
- 不要因为“只是色块占位”就放松 UI 标准；即使是占位版本，也应能准确验证商业化界面的结构、阅读顺序和状态表达。
- 每个核心机制都应有最小可验证场景或测试入口。
- 数值以 `Doc/` 中的设计为准；如需临时数值，应标记为 DEMO tuning，并避免冒充最终平衡。
- 不要为了早期 DEMO 引入复杂联网、账号、商城、存档云同步等系统。

## 设计红线

禁止把项目做成普通站桩卡牌对战。玩家操作的是卡牌，但屏幕体验必须像修士空中斗法。

必须保证：

- 每局有明确道途或流派方向。
- 中期能形成联动。
- 后期有爆发时刻。
- 飞剑、法宝、卡牌之间存在可感知的规则互动。

避免：

- 只有数值加减，没有构筑感。
- 无流派混搭导致体验发散。
- 战斗演出过静，缺少飞剑、法术、受击和连锁反馈。
- 早期把工作量投入到非核心外围系统。

## 商业化水墨 UI 快速判断

- 水墨方向不是把画面压暗，而是高留白、淡墨、宣纸质感、清楚轮廓和克制点染。
- UI 背景只托氛围，不承载选择信息；真正的选择信息应由卡片、节点、按钮和 Unity 文本组件表达。
- 如果页面需要三层以上蒙版、暗带、洗色或雾层才能勉强可读，应优先换成低细节、低对比、中心留白的新 UI 托底图，而不是继续叠蒙版。
- 商业化水墨 UI 应该做到：背景安静、卡片清楚、按钮明确、图多字少、状态统一、留白足够。
- 不应该做：大面积黑底压色、紫黑 / 蓝黑厚涂、现代玻璃面板、MMO 页游入口感、复杂背景叠复杂卡片、让生图模型生成中文正文或按钮字。
- 生 UI 背景时应明确禁止：角色、可读文字、Logo、UI 面板、卡框、强窗格、密集建筑、忙乱道具、横向黑带和重暗角。

## 工作流程

每次开始开发任务时：

1. 先读 `Doc/README.md`，确认文档入口。
2. 明确本次改动服务于 DEMO 的哪个核心目标。
3. 小步实现，保持可运行。
4. 完成后按当前验证约定检查构建和改动质量。
5. 更新必要的文档或开发备注。

当前常规验证约定：

- 默认不必每次启动 Unity Editor 或通过批处理打开工程验证。
- 常规代码 / 配置改动完成后，以 `dotnet build UNITY\CompileCheck\CompileCheck.csproj` 通过且 `0 警告 0 错误`、`git diff --check` 通过为主要验收口径；`git diff --check` 仅出现 Git 的 CRLF 提示可以接受。
- 只有用户明确要求，或改动依赖 Unity 实际导入、场景播放、资源序列化、Package Manager 解析、控制台运行时日志时，才额外调用 Unity Editor 验证。

如果本次任务包含视觉资源生产：

1. 先读 `Doc/08_UI美术与资源生产规范.md` 和 `Doc/11_资源制作单与开发记录.md`。
2. 明确资源用途，是服务战斗、UI、地图、Boss 还是宣传。
3. 对 UI 类资源，优先先完成布局和信息层级验证；未确认前可先用色块和占位块顶住，不必抢先生成正式图片。
4. 布局方案已经确认后，再优先生成当前 DEMO 最缺、最影响观感判断的正式资源，而不是追求一次做全。
5. 若会话可用内部挂载的 `image_gen` / `imagegen`，默认优先直接用它生成或编辑所需位图资源；如果不可用、调用失败或不适合当前需求，再使用 `imagegen2` skill 生图；只有这两类会话内能力都不适用时，才考虑其他工具链。
6. 生成后按规范落到项目目录，命名可追踪，并记录所用提示词或来源。

## 命令执行与工具绕路规则

本项目当前主要在 Windows / PowerShell 环境中开发。自动化代理执行命令时，应注意以下规则，避免把时间消耗在命令包装和转义问题上：

- 需要使用 Node.js、Python、Git，或处理表格、演示文稿、文档、PDF、图片与浏览器自动化前，应先调用工作区依赖加载器 `codex_app__load_workspace_dependencies`，取得本会话已挂载的可执行文件与包路径。裸命令不在当前 `PATH` 时，不得直接判断为“未安装”；优先使用加载器返回的绝对路径验证和执行，也不要把带版本号的缓存目录写死到脚本或规则中。只有加载器确认依赖缺失或返回路径不可用后，才考虑安装依赖或调整 `PATH`。
- 如果命令出现 `windows sandbox failed`、`helper_unknown_error`、`apply deny-read ACLs` 等 ACL / 沙盒错误，不要反复换复杂写法试错；先判断是否必要，必要时用同一条更简单、更窄范围的命令申请提权。
- 如果本地图片预览与多个无关路径同时报 `apply deny-read ACLs`，先检查 `%USERPROFILE%\.codex\.sandbox\setup_error.json` 和 `deny_read_acl_state.json`。若日志明确显示状态 JSON 解析失败，或状态文件为空 / 仅含 NUL 字节，应在获得权限后把损坏文件改名备份，让 Codex 自动重建，再用项目图片和普通沙盒命令各复测一次；不要修改项目图片或项目目录 ACL。
- ACL 提权只用于当前必要读取、图片尺寸检查、构建验证或 diff 检查；不要申请过宽前缀，不要把删除、重置、覆盖等风险操作混入提权命令。
- `apply_patch` 若因 Windows ACL 失败，不要多次重试同一补丁；可改用 PowerShell / here-string 做精确替换，写入后必须检查关键片段、保持 UTF-8、确保文件末尾换行，并运行 `git diff --check`。

- 在 PowerShell 中读取文件片段时，优先使用简单命令，例如 `Get-Content -LiteralPath ... -Encoding UTF8 | Select-Object -Skip ... -First ...`。不要在一行命令中混入复杂的 `$变量`、格式字符串和多层引号；这些内容很容易被外层 shell 提前展开或截断。
- 需要运行多行 Python、批量图片处理、批量文件整理等脚本时，优先使用 PowerShell here-string 管道：`@' ... '@ | python -`。不要把多行 Python 塞进 `python -c "..."`，尤其不要在其中混用 `\n`、三引号、f-string、反斜杠和中文路径。
- 如果只是查看或截取文本，不要为了行号美观构造复杂格式化命令。先稳定拿到内容，再决定是否需要行号。
- 命令失败时先判断失败类型：如果是参数、尺寸、引号、转义、路径或 shell 包装问题，应先修正命令本身；如果是网络、接口、权限或沙盒问题，再按会话权限规则申请升级或切换工具。
- 不要把一次性复杂命令当作长期可复用流程。凡是明显会复用的处理逻辑，应沉淀为脚本、工具函数或文档规则，减少后续重复绕路。

## 子代理并行规则

- 当任务存在两个或以上彼此独立、可并行，且不会修改同一文件、Unity 场景或 Prefab 的工作项时，允许并要求主代理主动使用子代理；简单任务、强串行任务或并行收益不明确时，不要为了开满并发而机械拆分。
- 子代理数量不得超过当前会话实际可用的空闲槽位，且最多同时启用 3 个。主代理必须保留一个槽位负责关键路径、任务调度、结果整合、冲突处理与最终验证。
- 适合并行的任务包括：独立文档或代码区域调查、互不重叠的模块实现、测试与构建检查、资源审计。存在前后依赖的步骤必须保持串行。
- 所有代理共享同一工作目录。多个代理不得同时修改同一文件、场景或 Prefab；主代理在分派前应划清文件与模块边界，并保留用户已有改动，不得用子代理结果覆盖来源不明的工作区变更。
- 主代理应跟踪子代理状态，完成后及时回收并复用并发槽位；子代理原则上不得继续递归拆分任务，除非主代理明确授权且仍满足总并发限制。
- 子代理完成任务并回传结果后，其并发槽位应立即视为可用。若仍有符合并行条件的剩余工作，主代理应优先复用上下文匹配的空闲子代理；不再适合后续任务的代理可以结束，并在空闲槽位上启用新的合适子代理。不得让已完成的代理闲置占位，同时把仍可独立推进的工作留在串行队列中。
- 计划模式的规划阶段只并行只读调查、方案验证和风险检查；进入执行阶段后，才允许并行实现。目标模式或持续执行的后续轮次中，应重新判断剩余工作是否满足并行条件，并继续遵循本节规则。
- 位图生成与编辑不适用本节的并行授权，继续严格遵守下方“生图与资源接入避坑”的禁止并发生图规则，任何生图任务都必须串行执行。

## 生图与资源接入避坑

- 生图请求硬性禁止并发。无论使用内部 `image_gen` / `imagegen`，还是备用 `imagegen2` skill，都必须一次只发起 1 张 / 1 个任务，等待完成、检查结果并落库或放弃后，才能开始下一张。不得用并行工具、批量并发或多路命令同时请求多张图。
- UI 图片资源尽量不要让生图模型直接生成中文正文、按钮字或数值说明。背景、器物、材质、按钮底图、烟雾和装饰可以生图；可交互文字、状态、数值和说明应由 Unity 文本组件渲染。
- `imagegen2` 生图时，尺寸可能存在模型或接口限制。若 16:9 的目标尺寸不能直接生成，可先生成满足限制的相邻尺寸，例如 1920x1088，再用图片处理裁切为 1920x1080 后落库。
- 透明底资源若接口不稳定，不要卡死在同一模型上反复重试。可按用途降级：
  - 烟雾、光效、简单 VFX 优先尝试透明 PNG。
  - 器物图若透明通道失败，可生成“宣纸底 + 墨雾边缘”的方图，让它自然融入 UI 容器。
  - 背景和大图卡优先用稳定的普通 PNG，再在 Unity 中通过遮罩、描边和半透明层完成融合。
- 生图接口出现 502、524、远端断开等临时错误时，只能在单任务串行前提下降低尺寸、降低质量或稍后重试；不要通过增加并发、重复多开请求或同时重试来碰运气。
- 资源落库时，优先同时放入展示目录与 Unity `Resources` 可加载目录，例如 `UNITY/Assets/Art/UI/` 与 `UNITY/Assets/Resources/Art/UI/`，并保持同名，方便运行时加载和后续替换。
- 纯装饰 UI 图层、烟气、雾效、光晕和遮罩默认关闭 `raycastTarget`，避免挡住按钮、命签、卡牌等交互区域。

## 规则沉淀约定

- 这类开发过程中反复出现、可复用、可稳定执行的通用规则，不要只留在对话里，应及时整理进 Markdown。
- 涉及项目级协作、流程、实现取向的长期规则，更新 `AGENTS.md`。
- 涉及当前版本、本轮 DEMO、某类功能反复适用的经验规则，可整理到 `Doc/` 中单独的补充备忘文档，并持续追加。
- 目标是减少后续重复判断、重复讨论和重复造轮子。

如果发现设计文档之间冲突：

- 优先遵循当前任务最相关的文档。
- 对 DEMO 工作，优先遵循 README 指向的 DEMO 第一阶段设计。
- 冲突仍无法判断时，选择更适合低成本 DEMO 验证的方案。
- 在提交说明或开发备注中记录取舍。

## 当前推荐 DEMO 切入点

当前原型向目标 DEMO 重构时，按 `Doc/10_DEMO重构实施计划.md` 推进：

1. 建立新运行状态、数据契约与旧流程兼容层。
2. 实现手工骨架加种子变化的三幕旅程图，以及节点间原子存档。
3. 合并根脚与所携的沉浸开局，接入事件、修炼、秘藏和因果标记。
4. 将起步能力收束为本命法器、心法技能和初始剑诀，并接入故事化筑基。
5. 将单敌人战斗迁移为有限多目标、自动攻击加点击锁定。
6. 重做左下到右上的普通战舞台，以及 Boss 正上方压屏构图。
7. 接入两只怪兽小 Boss、玄铁镇矿剑傀和完整三幕内容。
8. 串行生产并接入当前纵切真正缺少的战斗、地图、角色与 Boss 资源。

## 战斗商业化表现与资源降级规则

- 战斗首屏优先保证空间关系：顶部只承载生命、资源、敌方意图和阶段；中央必须保留完整的玩家承载物、敌方主体和攻击演算区；底部手牌台向中间收，不能吞掉脚下飞剑、法器或平台。
- 完整场景图作为区域战斗底图时，不再叠加大面积纯色矩形伪远景、伪云层或伪暗带；这类层会形成明显矩形块。需要动态氛围时，使用低 alpha 的独立云雾图片或轻量程序化漂移，并保证远景、中景速度不同。
- 角色背影的风感至少要有可见反馈：优先使用独立头发、衣带、衣摆层；正式分层资源缺失时，用低对比的剑痕 / 墨迹残影做程序化占位，不能用高亮几何条直接冒充正式角色部件。
- 攻击路径必须服务流派识别：万剑使用扇形或分叉弧线，雷剑使用短促折线 / 闪断，血剑使用弧钩与朱砂残线。通用直线只作为默认或敌方攻击回退。
- 生图 UI 底图不生成中文正文、按钮文字、数值、Logo 或图标；交互文本和状态数字由 Unity 文本组件渲染。生成图只负责材质、墨迹边缘、承托关系和装饰。
- 内置生图不可用时，使用 imagegen2 串行单张生成。透明资源优先尝试透明输出；若模型或账号不支持，改用 gpt-image-2 纯绿幕生成，再用 remove_chroma_key.py 本地抠图，并检查尺寸、RGBA、四角透明和 alpha bbox。
- 遇到 HTTP 524、模型未开放或透明通道失败时，停止重试并登记到专项缺口清单；不得用半成品替代正式资源。缺图位置必须有可读的纯色模块、描边模块或旧资源回退，保证功能验证不中断。
- 新生成资源要同时落到展示目录和 Resources 加载目录，文件名使用版本后缀，保留对应 Unity meta；专项文档记录用途、处理方式、提示词摘要和失败原因。
## 战斗首屏结构级重做规则

- 当用户截图反馈为“整体不成立”“完全不行”或明确要求大改时，禁止继续只调字号、透明度、锚点和几个像素；必须先重新判断顶部信息带、中央斗法空间、角色比例、底部操作台四个结构层，必要时直接拆掉旧结构重做。
- 战斗首屏评审顺序固定为：空间构图与对峙关系 -> 核心操作层级 -> 状态可读性 -> 动态反馈 -> 装饰材质。前一层未成立时，不得用继续叠边框、暗带、蒙版、雾层或小装饰掩盖。
- 新生成的战斗 UI 资源必须替换或合并旧结构，不得在旧手牌架、旧技能坞、旧状态框上继续叠一层新皮。底部手牌、行动按钮、神通 / 法器状态应优先落在同一个连续承载物上。
- 角色立绘不能作为无承载的悬空贴图直接摆放。玩家与敌人都必须有飞剑、法器、墨痕、平台或低对比能量承载面，并在同一战斗基线形成清楚的左右对峙。
- 战斗 UI 需要新资源时，按 imagegen -> imagegen2 的优先级串行单张生成；生成结果必须检查尺寸、主体比例、文字污染、透明通道、四角透明、alpha bbox 和暗 / 亮底合成效果。
- 生图若出现接口失败、透明通道失败、不可接受的绿边 / 黑底、错误文字或构图失控，只允许一次有明确目标的重试或确定性本地清理；仍不合格就登记到 `Doc/11_资源制作单与开发记录.md`，并用结构完整、信息可读的纯色 / 描边模块回退，不得接入半成品。
- 每次战斗首屏结构级重做完成后，除编译和 diff 检查外，还必须在专项文档记录：拆掉了什么旧结构、接入了什么新结构、资源来源 / 提示词摘要、失败与降级情况，以及下一张运行截图应重点验证的项目。
## Unity 商业化页面自动截图验证

以下流程用于 UI 结构级重做、跨页面流程改版、资源接入和布局可读性验收。普通逻辑 / 配置改动仍以 CompileCheck 和 diff 检查为主；只要结论依赖 Unity 实际布局、Canvas 渲染、Resources 导入或运行画面，就必须补跑本节验证。

### 当前首战后页面截图入口

- 截图工具：`UNITY/Assets/Editor/DemoPostBattleScreenshotTool.cs`。
- 工具会确定性推进：开局选择 -> 跳过首战结算 -> 战后所得 -> 择前路 -> 非战斗节点。
- 输出固定为项目根目录 `tmp/` 下 3 张 1920x1080 PNG：
  - `post_battle_reward_commercial.png`
  - `post_battle_route_commercial.png`
  - `post_battle_node_commercial.png`
- 工具只存在于 `Assets/Editor/`，可使用反射或测试性状态推进；这类辅助逻辑禁止进入正式运行时程序集和玩法代码。

从项目根目录执行：

```powershell
& 'D:\Unity Hub\Editor\Unity.exe' `
  -batchmode `
  -projectPath 'D:\MyGame\Path_of_Ten_Thousand_Ways\UNITY' `
  -executeMethod PathOfTenThousandWays.Demo.EditorTools.DemoPostBattleScreenshotTool.Capture `
  -logFile 'D:\MyGame\Path_of_Ten_Thousand_Ways\tmp\unity_postbattle_capture.log'
```

运行约定：

- 先读 `UNITY/ProjectSettings/ProjectVersion.txt`，确保使用同版本编辑器。当前项目锁定 `6000.3.17f1`；当前环境编辑器为 `D:\Unity Hub\Editor\Unity.exe`。
- 不要同时启动两个指向同一工程的 Unity Editor。命令外壳可能在 Unity 子进程结束前先返回；此时应查询并等待 `Unity.exe` 进程退出，再读取日志和截图，不要立刻重复启动。
- 批处理 UI 截图不得依赖 `ScreenCapture.CaptureScreenshot` 的异步落盘；无可见 GameView 时它可能只打印成功日志但不生成文件。统一使用临时 `ScreenSpaceCamera + RenderTexture + ReadPixels + File.WriteAllBytes` 同步读回。
- 每次切换状态后至少等待若干帧，让 `Start / Update`、LayoutGroup、CanvasScaler 和 Resources 加载完成后再截图。
- 截图写入 `tmp/`，不作为正式 Unity 资源，不复制到 `Assets/`，也不提交其预览缩略图。
- 新增或修改 Editor 截图工具后，必须保留 Unity 生成的 `.meta`，并确认批处理重新导入无编译错误。

验收命令：

```powershell
dotnet build UNITY\CompileCheck\CompileCheck.csproj
dotnet run --project UNITY\CompileCheck\CompileCheck.csproj
git diff --check
rg -n "error CS|Exception|NullReference|InvalidOperation|Aborting" tmp\unity_postbattle_capture.log
python -c "from PIL import Image; from pathlib import Path; [print(p.name, Image.open(p).size, Image.open(p).mode) for p in Path('tmp').glob('post_battle_*_commercial.png')]"
```

通过标准：

- CompileCheck 必须 `0 警告 / 0 错误`。
- 流程烟测必须通过；当前输出应包含 `Post-opening battle flow smoke check passed.`。
- `git diff --check` 只允许项目既定的 CRLF 提示。
- Unity 日志不得出现脚本编译错误、未处理异常、空引用或截图流程中止。
- 3 张图片必须真实存在且均为 `1920x1080 RGB/RGBA`，不能只凭日志中的 `Captured` 文案判定成功。
- 必须实际查看三张截图；仅有构建成功、文件尺寸或非空像素不能证明 UI 结构成立。

### 扩展截图入口规则

- 后续新增奖励页、Boss 前整备、结算页、商店或其他商业化关键状态时，优先扩展确定性 Editor 截图工具，一次覆盖完整连续流程，不依赖人工反复点击。
- 截图工具必须按用户真实阅读顺序捕获状态，不能只截单个组件或脱离上下文的 prefab。
- 截图工具应使用公开玩法接口推进；只有无法稳定到达的测试节点才允许在 Editor 程序集中使用反射，并在工具内显式校验前置状态，失败时抛出异常而不是静默继续。
- 自动截图工具属于验证基础设施。出现相同手工推进 / 截图逻辑两次以上时，应沉淀为共享辅助方法，不再复制粘贴新的状态机。

## 商业化 UI 通用结构与像素验收规则

- 商业化改版验收对象是完整体验链，不是单页截图。至少检查进入页、选择页、确认后页面和下一操作状态之间是否保持场景、材质、标题层级和交互语义连续。
- 战斗、奖励、择前路、节点、整备和结算职责不同，不得长期复用同一套“通用暗色卡 + 点击领取”骨架。共用的应是色板、纸张材质、间距、印章语言和路径信息，不是相同信息结构。
- 页面只能有一个主承载层。禁止“全屏外框 -> 中央大卡 -> 左右小卡 -> 卡内再套卡”的嵌套面板；摘要、路径和状态优先使用无框信息带、细线分区或留白组织。
- 完整场景图只负责气氛和空间连续性。场景上方可以有一层轻宣纸洗色，但不能再叠大面积伪远景、深灰横条或高纹理 UI 图压住文字。
- 高纹理资源不得直接拉伸到小型文字摘要区。若贴图让摘要区出现条纹、脏灰、局部过暗或文字对比不稳定，立即退回纯色浅纸 + 低 alpha 描边，不通过继续叠蒙版补救。
- 选择页先看选择单位，再看解释。奖励卡突出“得到什么 / 如何改变构筑”；路线卡突出“去哪里 / 节点顺序 / 稳险与收益”；普通节点突出“当前发生什么 / 此处所求 / 下一步做什么”。
- 三选一或四选一的卡片必须固定宽高、同一基线、稳定间距；候选数量变化不得拉宽、压扁或改变主图比例。
- 路线节点序列必须在固定宽度内验证 2、3、4 节点情况。节点类型、顺序和连线是一级信息，路线长文不得把节点挤出卡片。
- 小型摘要不显示全量运行数据。只保留当前步位、下一节点、气血、牌组、主修 / 起势和首要法器；全量地图与完整道基留给详情页或悬停层。
- 所有动态长文都要有稳定容器和溢出策略。标题、规则、路线说明优先缩短；仍可能超长时设置换行与 `VerticalWrapMode.Truncate`，不能让文本盖住按钮或下一段内容。
- 装饰图层、场景洗色、雾层、印记和细线统一关闭 `raycastTarget`。截图成立后仍要检查交互层，不能让不可见装饰挡住卡片和按钮。
- 结构级改版的评审顺序固定为：页面职责与阅读顺序 -> 主选择 / 主行动 -> 状态与路径可读性 -> 场景连续性 -> 动态与装饰。前一层未成立时，不继续加边框、贴图、烟雾和小装饰。
- 每轮截图后要按截图暴露的问题继续迭代。若发现深色旧框、占位方块、纹理污染、文字溢出、同高失效或 UI 重叠，目标仍未完成，不能因为编译通过就收口。
- 专项开发记录必须写清：拆掉的旧结构、最终结构、复用 / 新增资源、失败与降级、自动截图文件名、实际发现的问题和修正结果。
